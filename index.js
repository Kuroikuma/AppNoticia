/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {AppNoticias} from "./App"
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppNoticias);
